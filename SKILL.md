---
name: piki
description: >
  宝可梦对战知识库助手，同时负责向 Obsidian vault 写入竞技对战笔记。
  只要用户提到以下任何内容，都必须立即启用此 skill：宝可梦对战、单打（Singles）、
  双打（Doubles）、VGC、Pokémon Champions、配招、特性、招式、克制、阵容、排位赛、
  BP（Battle Points）、EV/IV、性格（Nature）、道具（Items）、超级进化（Mega）、
  Z招式、太晶（Tera）、动态巨化（Dynamax）、以及任何具体宝可梦名称（无论中英文）。
  用户想把对战知识整理成 Obsidian 笔记时同样使用。**当用户给出对战视频/录像/
  YouTube/B站链接，或队伍截图（Team Preview/队伍编成/rental 租借队码）并希望
  整理成阵容笔记时，也必须用此 skill**——解析视频字幕与图片内容、还原阵容、
  提炼玩法技巧/注意事项/对策，并把图片作为附件嵌入笔记。不要因为问题
  「看起来很简单」而跳过此 skill——宝可梦对战知识需要对战专业数据来源，不能靠记忆回答。
---

# piki — 宝可梦对战知识库助手

**适用语境**：Pokémon Champions 单打/双打主流竞技对战（规则演变时以数据库实时数据为准）。

**规则要点（重要，作答与建卡均须遵守）**：
- **Champions 没有太晶化（Terastallization / テラスタル）概念**。不要在 Champions 相关的回答或笔记中引入太晶机制；涉及第九世代太晶的内容仅作历史/其他规则对照，不作为 Champions 默认前提。
- **默认对战等级为 50 级**。涉及伤害、数值、确定数等计算时，无特殊说明一律按 50 级处理。

**输出语言**：所有回答用**中文**。

---

## 准备工作（每次调用时执行一次）

1. **加载 Web 工具**：执行 `ToolSearch("select:WebFetch,WebSearch")` 以确保这两个工具可用。
2. **读取数据源索引**：读取 `references/sources.md`（此文件与 SKILL.md 同目录），了解各数据库的查询方式。

---

## 核心流程（每次回答问题时执行）

### 第〇步：输入分流（视频/图片建阵容）

若用户的输入包含**视频链接、对战录像，或队伍图片/截图**，且意图是整理阵容，
先读取 `references/video-ingest.md` 走「阵容采集」前置流程：

1. **视频** → 运行 `python "<skill目录>/scripts/fetch_video.py" "<URL>" "<scratchpad目录>"`，
   读取它打印的 `output_file`（含元数据/简介/字幕）。从简介（rental 码/队伍列表）与
   字幕还原 6 只构成，并从字幕提炼**玩法技巧 / 注意事项 / 对策**。
2. **图片** → 读图识别 6 只与可见配置；把**真实图片文件**存入 `D:\Vaults\AIO\Attachments\`
   （取不到原文件就请用户补文件/路径，不重绘失真图），正文用 `![[文件名]]` 嵌入。
3. **还原不出阵容**（无字幕且简介无队伍信息）→ 明确请用户补一张队伍截图，**不编造阵容**。

采集到的信息随后并入下面常规流程：用「第二步」查权威数据库**核对名称/配招**，
经「第四步」确认后用 `templates/阵容.md`（已含上述章节）写卡。

### 第一步：查 Vault 已有笔记

在本地 vault 搜索相关已有内容，目的是：避免重复建卡、找到可更新的已有笔记、串联知识网络。

```
搜索位置：D:\Vaults\AIO\（重点：Inbox\ 和已有的 piki 笔记）
工具：Grep（按关键词/中文名）或 Glob（按文件名）
搜索词：问题中的宝可梦名、招式名、特性名、主题词（中英文均尝试）
```

记录找到的相关笔记文件路径，供第四步建卡时使用（更新 vs 新建决策依据）。

### 第二步：查权威数据库

**根据问题类型选择数据源**（优先级参见 `references/sources.md`）：

| 需求 | 首选来源 | 备选 |
|---|---|---|
| 中文名/属性/基础数据 | 52poke（WebSearch `site:wiki.52poke.com <名>` → WebFetch） | Bulbapedia |
| 详尽机制/招式效果 | Bulbapedia (`/wiki/<Name>_(move)`) | Serebii |
| Champions 排位使用率 | pokechamdb (`/zh-Hans/pokemon/<name>?season=M-3&format=single`) | pokedb.tokyo |
| VGC 阵容/战术思路 | vgcguide (`/teambuilding`, `/battling`) | limitlessvgc |
| 各世代数据速查 | Serebii (`/pokedex-sv/<name>/`) | Bulbapedia |

**⚠️ 52poke 访问提示**：直接 WebFetch `wiki.52poke.com` 常返回 403。改用：
```
WebSearch: site:wiki.52poke.com <中文名称>
```
取到具体页面 URL 后再 WebFetch。

**查询范围**：针对问题中的每个核心实体（宝可梦/招式/特性/道具），至少查 1 个来源。
竞技数据类问题（使用率/配招）必须查 pokechamdb，不能仅凭记忆回答。

### 第三步：作答

综合第一、二步信息回答用户问题，遵循以下原则：

- **聚焦对战**：只写与竞技对战直接相关的内容（不写宝可梦生活习性、图鉴描述、出现地点）
- **精简易懂**：避免罗列大量数据，突出对决策有帮助的核心信息
- **专有名词格式**：首次出现时用 `中文名 (English・日本語名)` 格式，例如：
  - 宝可梦：`喷火龙 (Charizard・リザードン)`
  - 招式：`拍落 (Knock Off・はたきおとす)`
  - 特性：`猛火 (Blaze・もうか)`
  - 道具：`气势披带 (Choice Scarf・こだわりスカーフ)`
- **数据来源**：若引用排位数据，说明赛季和模式（如「M-3 赛季单打数据」）

### 第四步：询问是否建卡

回答结束后，主动询问：

> 「是否将本次问答整理成 Obsidian 笔记？」

并给出具体方案供选择，方案格式：

```
方案 A（推荐）：
  - 新建《喷火龙 (Charizard).md》（类型：宝可梦）——记录对战定位和配招
  - 新建《猛火 (Blaze).md》（类型：特性）——记录特性机制

方案 B（简化）：
  - 更新已有《宝冠军阵容.md》，追加喷火龙配招说明

方案 C：暂不整理
```

等用户选择后再执行写卡。**不要在用户确认前自动写入任何文件。**

---

## 写卡流程（用户确认后执行）

**写卡前必读**：读取 `references/note-conventions.md` 和对应的 `templates/<类型>.md`。

### 选择模板

| 用户问题涉及 | 使用模板 |
|---|---|
| 具体宝可梦的对战知识 | `templates/宝可梦.md` |
| 招式的效果/用途 | `templates/招式.md` |
| 特性的机制/价值 | `templates/特性.md` |
| 机制/概念/基础规则 | `templates/基础.md` |
| 阵容构成/战术 | `templates/阵容.md` |
| 对战操作/决策技巧 | `templates/技巧.md` |

模板路径：与 SKILL.md 同目录的 `templates/` 文件夹。

### 写卡规则

1. **路径**：所有新笔记写入 `D:\Vaults\AIO\Inbox\`
2. **文件名**：`<中文名>.md`，**只用中文**（如 `拍落.md`），特殊字符用空格代替。**「宝可梦」类型笔记须加模式后缀**：`<中文名>（单打）.md` 或 `<中文名>（双打）.md`（如 `阿罗拉九尾（单打）.md`）；招式/特性/基础/阵容不加，详见 `references/note-conventions.md`
3. **alias**：frontmatter 的 `alias` 字段填写英文名（必填）、日文名（必填）、别名（选填）
4. **日期**：`date` 字段填写当前会话日期（今天是 2026-06-27，下次调用时以实际日期为准）
5. **双链**：专有名词统一用 `[[中文名]]` 双链（如 `[[喷火龙]]`、`[[拍落]]`），不在双链中附英文
6. **标签**：必须含 `宝可梦对战` + 恰好一个类型标签；若 6 个类型均不合适，向用户提案新标签
7. **中枢笔记**：若 `D:\Vaults\AIO\Inbox\宝可梦对战.md` 不存在，在写第一张笔记前先创建它（模板见 `references/note-conventions.md`）

### 更新已有笔记

- 读取现有笔记内容，在合适位置追加/修改
- 保持原有 frontmatter，只更新 `date` 和 `related`
- 不改变笔记的原子性边界（若需补充超出原有范围的内容，新建独立笔记并互链）

---

## 标签扩展流程

若 `宝可梦/招式/特性/基础/阵容/技巧` 六个标签都不适合：

1. 提出新标签提案，格式：「建议新增标签 `#<名称>`，适用于……是否采纳？」
2. 用户采纳后：
   a. 在 `references/note-conventions.md` 的标签表中添加新行
   b. 在本笔记中使用该新标签

---

## 参考文件说明

| 文件 | 何时读取 |
|---|---|
| `references/sources.md` | 每次调用开始时，了解各数据源查询方式 |
| `references/video-ingest.md` | 输入含视频链接/图片、要建阵容卡时（第〇步） |
| `references/note-conventions.md` | 确认写卡后，生成笔记前（含附件规则） |
| `templates/<类型>.md` | 确认写卡后，根据笔记类型选择对应模板读取 |
| `scripts/fetch_video.py` | 第〇步抓取在线视频的元数据/简介/字幕 |
